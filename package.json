{
  "name": "careerprint-monorepo",
  "private": true,
  "scripts": {
    "dev:linkedin": "pnpm --filter linkedin dev",
    "dev:facebook": "pnpm --filter facebook dev",
    "dev:x": "pnpm --filter x dev",
    "build:linkedin": "pnpm --filter linkedin build",
    "build:facebook": "pnpm --filter facebook build",
    "build:x": "pnpm --filter x build"
  }
}
